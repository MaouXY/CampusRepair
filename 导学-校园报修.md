# 导学：校园报修

## 已确认输入

- 简称：校园报修
- 项目定位：基于 Spring Boot 3、Vue 3、MyBatis Plus、JWT、RustFS、LangChain4j、Milvus/RAG 的校园智能报修与工单调度系统
- 求职方向假设：后端为主，兼顾 AI 工程化与前端联调

## 1. 前置知识（面试高频标注）

| 知识点 | 为何需要 | 在本项目中的位置 | 高频度 |
| --- | --- | --- | --- |
| Spring Boot 分层架构 | 解释 Controller、Service、Mapper、DTO 的职责边界 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module` | 高 |
| MyBatis Plus 与事务 | 工单创建、派单、评价需要保证数据一致性 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ticket/service/TicketService.java` | 高 |
| JWT 与角色权限 | 学生、维修员、管理员的数据范围不同 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/common/security` | 高 |
| 状态机 | 防止工单被重复派单、跨状态处理、非法确认 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ticket/state/TicketStateMachine.java` | 高 |
| 运行日志与业务留痕 | 面试会追问故障定位、审计、降级原因是否可观测 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/audit/service/OperationAuditService.java` | 高 |
| RustFS/S3 对象存储 | 报修图片和维修结果图片不能直接塞进数据库 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/file/service/FileService.java` | 中 |
| LangChain4j 与结构化输出 | AI 预分析需要 JSON 解析、校验、兜底和调用记录 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ai/service/TicketAiAnalysisService.java` | 高 |
| RAG 检索增强 | 避免模型凭空生成维修建议，提供知识依据 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/rag/service/RagKnowledgeService.java` | 高 |
| Vue Router 与路由守卫 | 前端三类角色入口和页面权限控制 | `CampusRepairWeb/src/router/index.ts` | 中 |
| Axios 拦截器 | 统一处理 token、错误提示、401 退出登录 | `CampusRepairWeb/src/utils/request.ts` | 中 |

## 2. 重点亮点与学习顺序（先看这个）

- 工单闭环状态机：重要在于它把提交、派单、接单、处理、确认、返工、评价这些动作收敛成可校验的状态迁移。先看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ticket/state/TicketStateMachine.java`，再看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ticket/service/TicketService.java` 中的 `transition`、`requireActionAllowed`、`addFlow`。
- 三角色权限隔离：重要在于同一个工单详情对学生、维修员、管理员的可见范围不同。先看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/common/security/JwtAuthenticationFilter.java`，再看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ticket/service/TicketService.java` 的 `detailForStudent`、`ensureAssignedToWorker`、`ensureTicketReadable`。
- AI 预分析与降级可观测：重要在于 AI 不能阻塞主流程，同时解析失败、调用失败都要有运行日志和业务记录。先看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ai/service/TicketAiAnalysisService.java`，再看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ai/service/TicketAiPreAnalysisTrigger.java`。
- RAG 知识库检索：重要在于从文档切片、向量同步、Milvus 搜索、关键词兜底、重排形成检索链路。先看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/rag/service/RagKnowledgeService.java`，再看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/rag/service/MilvusRagVectorService.java` 和 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/rag/service/RagRerankService.java`。
- 文件上传和业务绑定：重要在于图片先临时上传，再绑定到报修或维修结果，避免对象存储成功但业务关系错误。先看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/file/service/FileService.java`，再看 `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ticket/service/TicketService.java` 的图片绑定方法。
- 前后端角色工作台：重要在于 Vue 页面不是单一后台，而是学生、维修员、管理员、大屏四类视图。先看 `CampusRepairWeb/src/router/index.ts`，再看 `CampusRepairWeb/src/layouts/AppShell.vue`、`CampusRepairWeb/src/views/student/StudentHomeView.vue`、`CampusRepairWeb/src/views/admin/AdminTicketsView.vue`。

## 3. 必备知识点

- 搞懂工单状态：`PENDING_REVIEW`、`ASSIGNED`、`PROCESSING`、`WAITING_CONFIRM`、`COMPLETED`、`REJECTED`、`RETURNED`。
- 搞懂为什么状态迁移要集中在状态机，而不是在每个接口里散写 `if`。
- 搞懂 `@Transactional` 在创建工单、绑定图片、派单、评价中的作用。
- 搞懂 JWT 认证、`CurrentUserProvider`、角色枚举和接口级权限校验的关系。
- 搞懂文件上传为什么先落 RustFS，再写 `file_metadata`，以及临时文件绑定业务 ID 的意义。
- 搞懂 AI 输出为什么必须 JSON 解析和服务端校验，不能直接相信大模型文本。
- 搞懂 RAG 的数据链路：知识文档 -> 切片 -> embedding -> 向量库 -> 召回 -> rerank -> 组装提示词。
- 搞懂业务日志、审计日志、AI 调用记录、运行日志分别解决什么问题。
- 搞懂前端路由守卫如何根据角色跳转默认首页并拒绝越权页面。
- 搞懂集成测试如何覆盖主流程、越权、非法状态和事务回滚。

## 4. 推荐阅读（结合仓库）

| 主题 | 建议阅读的代码/文档位置 | 预计时间 | 读完能回答什么 |
| --- | --- | --- | --- |
| 项目总体背景 | `README.md`、`doc/02_需求分析.md`、`doc/03_系统规划与概要设计.md` | 20 分钟 | 这个系统解决什么校园后勤问题，为什么要拆学生、维修员、管理员三类角色 |
| 后端模块设计 | `doc/06_后端开发文档.md`、`CampusRepairApi/pom.xml` | 25 分钟 | 技术栈如何选型，Spring Boot、MyBatis Plus、JWT、RustFS、LangChain4j 分别负责什么 |
| 工单主流程 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ticket/service/TicketService.java` | 60 分钟 | 从提交报修到评价完成，后端怎样校验权限、更新状态、写流转记录 |
| 状态机 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ticket/state/TicketStateMachine.java` | 25 分钟 | 如何防止重复派单、提前评价、未分配维修员接单等非法动作 |
| 权限认证 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/common/security/JwtAuthenticationFilter.java`、`CampusRepairApi/src/main/java/com/maou/apptemplateapi/common/security/JwtTokenService.java` | 35 分钟 | token 如何解析成当前用户，接口内如何做角色和数据范围校验 |
| 统一异常 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/common/exception/GlobalExceptionHandler.java` | 20 分钟 | 业务异常、参数异常、未预期异常分别如何返回和记录日志 |
| 文件模块 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/file/service/FileService.java` | 35 分钟 | 图片上传如何限制类型和大小，RustFS 失败如何保留异常栈 |
| AI 预分析 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/ai/service/TicketAiAnalysisService.java`、`doc/04_AI模块设计方案.md` | 60 分钟 | AI 如何结合工单描述、图片、RAG 片段和维修员列表生成派单建议 |
| RAG 知识库 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/rag/service/RagKnowledgeService.java`、`CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/rag/service/MilvusRagVectorService.java` | 60 分钟 | 文档切片、向量同步、Milvus 召回、关键词检索兜底如何协同 |
| 统计大屏 | `CampusRepairApi/src/main/java/com/maou/apptemplateapi/module/stats/service/StatsService.java`、`CampusRepairWeb/src/views/screen/ScreenView.vue` | 30 分钟 | 后端怎样聚合今日新增、处理中、完成数、分类分布，前端怎样展示 |
| 前端路由权限 | `CampusRepairWeb/src/router/index.ts`、`CampusRepairWeb/src/stores/auth.ts` | 35 分钟 | 前端如何根据 token 和角色限制页面访问 |
| 前端请求封装 | `CampusRepairWeb/src/utils/request.ts`、`CampusRepairWeb/src/api/ticket.ts` | 25 分钟 | Axios 如何统一加 token、处理业务 code、超时和 401 |
| 测试用例 | `CampusRepairApi/src/test/java/com/maou/apptemplateapi/TicketFlowIntegrationTest.java`、`CampusRepairApi/src/test/java/com/maou/apptemplateapi/module/ticket/state/TicketStateMachineTest.java` | 45 分钟 | 如何证明工单主流程、非法状态、越权和事务回滚都被覆盖 |
| 数据库脚本 | `CampusRepairApi/sql/01_schema_base.sql` 到 `CampusRepairApi/sql/12_seed_demo_data.sql` | 45 分钟 | 数据表如何支撑用户、工单、AI、RAG、审计和演示数据 |

## 5. 自学提醒

- 若某文件或原理看不懂，请继续追问 AI；本 skill 负责给学习路径与题目，不提供逐行讲解。建议追问时带上具体文件路径、方法名和你卡住的代码片段，这样能更快定位到设计意图。

## 6. 项目技术定位

- 倾向：交叉，后端 + AI 工程化 + 前端联调。依据是后端承担认证、工单状态、事务、文件、AI/RAG、统计聚合等核心复杂度，前端承担三角色工作台与大屏展示，AI 模块又引入结构化输出、RAG 检索和降级可观测。

## 7. 核心原理解析

### 7.1 工单状态闭环

- 问题 -> 校园报修不是简单 CRUD，工单会被学生、管理员、维修员连续操作，如果状态控制分散，容易出现已完成工单再次派单、未接单直接提交结果、非本人查看工单等问题。
- 机制 -> 项目用 `TicketStateMachine` 定义动作到状态的映射，用 `TicketService` 统一调用 `transition`，在事务中更新主表、写流转表、写操作审计。
- 在本项目中的落点 -> `TicketStateMachine.canTransition` 控制合法迁移，`TicketService.ensureTransitionAllowed` 负责拒绝非法动作，`RepairTicketFlow` 保存每次状态变化，便于详情页展示和问题追溯。

### 7.2 三角色权限隔离

- 问题 -> 学生只能看自己的报修，维修员只能看分配给自己的工单，管理员可以管理全量工单。只靠前端隐藏菜单不可靠。
- 机制 -> 后端通过 JWT 解析出 `CurrentUser`，接口进入 Service 后再次校验角色和数据归属。前端路由守卫只是用户体验层面的第一道门。
- 在本项目中的落点 -> `JwtAuthenticationFilter` 解析 token，`TicketService.requireRole` 做角色校验，`detailForStudent`、`ensureAssignedToWorker`、`ensureTicketReadable` 做数据范围校验。

### 7.3 AI 结构化输出与兜底

- 问题 -> 大模型返回可能不是合法 JSON，也可能建议不存在的维修员或不合规优先级；如果直接返回前端，会影响派单决策和演示稳定性。
- 机制 -> 项目用系统提示词限定 JSON schema，后端用 `ObjectMapper` 解析，再把优先级、风险等级、维修员 ID 做归一化校验。解析失败或调用失败时写 AI 任务失败记录，并保存规则兜底分析。
- 在本项目中的落点 -> `TicketAiAnalysisService.analyzeTicketInternal` 串起 RAG、图片、AI 调用、任务记录、异常日志和 fallback，日志里包含 `scenario`、`ticketId`、`aiTaskId`、`studentId`。

### 7.4 RAG 检索增强

- 问题 -> 校园维修建议需要基于知识库和历史规范，否则模型容易编造处理方案，尤其是漏电、停水、网络故障等场景。
- 机制 -> 管理员维护知识文档，后端按 chunk size 和 overlap 切片，优先调用 Milvus 向量检索，失败或没有命中时使用数据库关键词评分，再通过 rerank 重新排序。
- 在本项目中的落点 -> `RagKnowledgeService.rebuildChunks` 负责切片和向量状态更新，`search` 负责 Milvus 召回、数据库兜底和 rerank，`TicketAiAnalysisService` 将召回片段加入 AI prompt。

### 7.5 文件上传与业务绑定

- 问题 -> 报修图片和维修结果图片体积大，不能直接存数据库；但只保存 URL 又无法管理上传人、业务归属和越权访问。
- 机制 -> 文件先上传到 RustFS，数据库保存 `FileMetadata`。上传成功时先标记为 `TEMP`，工单创建或维修结果提交时再绑定成 `TICKET_REPORT_IMAGE` 或 `TICKET_RESULT_IMAGE`。
- 在本项目中的落点 -> `FileService.upload` 负责 RustFS 上传和元数据入库，`requireBindableFiles` 校验文件存在、上传人一致、未被其他业务绑定，`TicketService.bindReportImages` 和 `bindResultImages` 关联到工单。

## 8. 关键设计决策

| 决策点 | 备选 | 取舍 | 风险 | 验证 |
| --- | --- | --- | --- | --- |
| 工单状态控制 | 分散在各接口写条件判断 / 集中状态机 | 选择状态机，便于统一维护和测试 | 新增状态时必须同步动作规则 | `TicketStateMachineTest.java` 和 `TicketFlowIntegrationTest.java` |
| AI 是否阻塞主流程 | 同步等待 AI / 提交后触发预分析 | 选择提交后触发，保证报修主链路稳定 | AI 结果可能晚于人工审核 | 观察 AI task 状态和工单详情最新分析 |
| AI 输出处理 | 直接返回模型文本 / JSON schema + 服务端解析 | 选择结构化解析，便于前端展示和派单 | 模型不合规时会走兜底，建议质量下降 | 构造非法 JSON 或关闭模型服务测试 fallback |
| RAG 检索 | 只用关键词 / Milvus 向量 + 关键词兜底 + rerank | 选择组合检索，兼顾语义召回和离线演示 | 向量库不可用时召回质量下降 | 关闭 Milvus 后确认数据库关键词检索仍返回结果 |
| 图片存储 | Base64 入库 / RustFS 对象存储 | 选择对象存储，数据库只存元数据 | 对象存储失败会导致上传失败 | 模拟 RustFS 不可用，检查 `log.error` 和业务错误码 |
| 权限控制 | 只靠路由守卫 / 前后端双层控制 | 选择后端强校验、前端做体验优化 | 后端漏校验会造成越权 | 集成测试覆盖学生越权查看和维修员越权接单 |
| 日志策略 | 只写数据库业务记录 / 业务记录 + 运行日志 | 选择运行日志优先保留异常栈，数据库做业务留痕 | 日志字段不足会影响排障 | 检查 catch、fallback、第三方失败是否包含 `scenario` 和关键 ID |
| 统计数据 | 前端拉列表自行聚合 / 后端返回聚合 DTO | 选择后端聚合，减少前端重复计算 | 大数据量下聚合查询可能慢 | 对 `StatsService` 增加时间范围、索引和压测（待测） |

## 9. 量化与验证（含待测，建议）

- 工单主流程正确性：使用 `CampusRepairApi/src/test/java/com/maou/apptemplateapi/TicketFlowIntegrationTest.java` 跑完整链路，验证提交、派单、接单、提交结果、评价后的最终状态和流转条数。
- 状态机边界：使用 `CampusRepairApi/src/test/java/com/maou/apptemplateapi/module/ticket/state/TicketStateMachineTest.java` 补充每个动作的 allowed from status，非法迁移应返回错误码 `9602`。
- 权限隔离：构造不同学生 token、未分配维修员 token、管理员 token，分别访问同一工单，统计越权请求是否全部被拒绝，目标是越权通过数为 0（待测）。
- AI 可用性：分别测试正常模型响应、非法 JSON、模型超时、RAG 空召回、图片读取失败。指标建议记录 AI 成功率、fallback 次数、平均耗时、解析失败次数，当前数值待测。
- RAG 检索质量：准备 20 条典型维修问题，人工标注期望知识条目，对比 Milvus 开启和关闭时的 Top 3 命中率。指标为 Recall@3 和平均检索耗时，当前数值待测。
- 文件上传稳定性：用 1MB、5MB、超限文件、非法 MIME、RustFS 不可用场景测试。关注上传成功率、错误码、日志是否包含 uploaderId、objectKey、bucket、原始文件名，当前性能数据待测。
- 统计接口性能：在测试库插入 1 千、1 万、5 万条工单，对 `admin-stats-overview` 和大屏接口压测。建议用 JMeter 或 k6，记录 P95 响应时间、数据库慢查询、分类分布聚合耗时，当前数值待测。
- 前端体验验证：用学生、维修员、管理员三个演示账号登录，检查默认路由、菜单、详情页、按钮状态和 401 重新登录流程。建议用 Playwright 写关键路径自动化，当前自动化覆盖待测。
