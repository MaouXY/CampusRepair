# 面经：校园报修

## 项目简介（简历可用，1～2 句）

我做的是一个基于 Spring Boot 3、Vue 3、MyBatis Plus、JWT、RustFS 和 LangChain4j 的校园智能报修与工单调度系统，覆盖学生报修、管理员审核派单、维修员处理、学生确认评价、数据大屏和 AI/RAG 辅助分析。项目重点不是简单 CRUD，而是把工单状态机、三角色权限隔离、文件对象存储、AI 结构化输出兜底和运行日志可观测做成一套可演示、可追溯的工程闭环。

## 简历 bullet（4～6 条）

- 设计并实现学生、维修员、管理员三角色工单闭环，使用集中状态机约束提交、派单、接单、处理、确认、返工、评价等动作，减少非法状态流转风险。
- 基于 JWT、Spring Security 和 Service 层数据归属校验实现权限隔离，覆盖学生只能看本人报修、维修员只能处理分配工单、管理员管理全量工单。
- 集成 RustFS/S3 文件存储，采用临时上传再业务绑定的方式管理报修图片和维修结果图片，并在上传失败、绑定失败时记录运行日志和上下文。
- 封装 LangChain4j 工单 AI 预分析链路，结合 RAG 知识片段、报修图片和维修员列表输出分类、优先级、风险和派单建议，解析失败时写任务记录并走规则兜底。
- 建设 RAG 知识库管理能力，支持知识文档切片、Milvus 向量同步、数据库关键词兜底和 rerank 重排，为维修建议提供可追溯依据。
- 编写工单主流程集成测试和状态机测试，覆盖完整流转、跨角色越权、重复派单、图片绑定失败事务回滚等关键风险点。

## 面试问题（按频率排序）

### Q1（频率：高）[后端/工单状态机]

**面试官问题**：你这个校园报修系统为什么需要状态机？它解决了什么问题？

**口播版（第一人称，≥150 字，完整 STAR）**

我当时面对的场景是，校园报修不是单表增删改查，一个工单会经历学生提交、管理员审核派单、维修员接单处理、学生确认评价，如果每个接口自己写状态判断，很容易出现重复派单、未接单直接提交结果、已完成工单再次返工这类问题。我的任务是把状态流转变成可维护、可测试的规则，而不是靠页面按钮隐藏来保证。具体实现上，我在 `TicketStateMachine` 里用动作枚举和状态枚举维护合法迁移，比如 `ASSIGN` 只能从 `PENDING_REVIEW` 或 `RETURNED` 到 `ASSIGNED`，然后在 `TicketService.transition` 里统一校验、更新主表、写流转表和审计记录。这样结果是业务接口只表达动作意图，状态合法性由一处收敛，后续新增返工、催办、退回时也更容易扩展和测试。

**追问 1**：如果管理员重复派单，或者维修员接了不属于自己的工单，你怎么防？

**口播版（≥150 字）**：我会把这类问题拆成两个层面处理：第一层是状态合法性，第二层是数据归属。比如管理员重复派单时，第一次派单后工单状态已经变成 `ASSIGNED`，再次调用派单接口会进入 `requireActionAllowed`，状态机发现 `ASSIGN` 不允许从 `ASSIGNED` 触发，就返回工单状态非法。维修员越权接单则不是状态机能完全解决的，因为状态可能是合法的，但人不合法，所以我在 `ensureAssignedToWorker` 里校验当前登录维修员 ID 是否等于工单的 `assignedWorkerId`。如果不一致，会写包含 `ticketId`、`workerId`、`assignedWorkerId` 的运行日志并抛业务异常。这个设计的结果是，状态和权限边界各自独立，测试里也能分别覆盖重复派单和跨角色访问。

**追问 2**：如果让你重构这块，你会怎么演进？

**口播版（≥150 字）**：如果继续演进，我会保留集中状态机这个方向，但会把状态迁移规则进一步配置化或者表格化，让每个动作的前置状态、目标状态、允许角色、是否写审计、是否触发通知形成统一元数据。现在项目里状态机负责状态规则，角色和业务校验主要在 Service 方法里，短期足够清晰，但动作越来越多时，可能会出现校验散落的问题。我会先补齐状态机单元测试矩阵，再把 `requireActionAllowed`、`requireRole`、`ensureAssignedToWorker` 这些逻辑抽象成动作策略。结果上希望新增一个动作时，只需要声明规则和实现少量业务副作用，就能自动获得非法状态拒绝、权限校验、流转记录和运行日志能力。

### Q2（频率：高）[权限/JWT/数据隔离]

**面试官问题**：项目里学生、维修员、管理员的权限是怎么做的？

**口播版（第一人称，≥150 字，完整 STAR）**

我做权限时的背景是，这个系统有明显的数据隔离要求：学生只能看自己的报修，维修员只能看分配给自己的工单，管理员才可以看全量工单。如果只靠前端路由隐藏菜单，接口仍然可能被直接调用，所以我的目标是做前后端双层控制，但以后端为准。后端里我用 JWT 保存用户 ID、登录类型、角色和组织上下文，`JwtAuthenticationFilter` 解析 `Bearer Token` 后放入 Spring Security 上下文，再由 `CurrentUserProvider` 给业务层使用。Service 层会根据场景调用 `requireRole`、`detailForStudent`、`ensureAssignedToWorker` 等方法做角色和数据归属校验。前端的 `router.beforeEach` 根据 token 和角色限制页面访问，只负责体验。这样最终即使绕过前端直接请求接口，后端仍能拒绝越权。

**追问 1**：为什么不只用 Spring Security 的注解，比如 `@PreAuthorize`？

**口播版（≥150 字）**：我没有只依赖 `@PreAuthorize`，主要是因为这个项目的权限不只是角色级别，还包括数据级别。比如维修员访问 `/worker/tickets/{id}`，角色是 `WORKER` 只能说明他是维修员，不能说明这个工单分配给了他；学生访问工单详情也是同理，必须比较 `studentId` 和当前用户 ID。所以我的做法是让过滤器和 Spring Security 解决“你是谁”和“你的角色是什么”，再在 Service 层解决“这条数据是不是你的”。这样虽然代码会多一些显式校验，但每个业务场景都能记录清楚的 `scenario`、`ticketId`、`userId`、`roleCode`。结果是权限问题更容易排查，集成测试也能用不同 token 构造越权场景来验证。

**追问 2**：token 失效或解析失败时，你怎么保证前后端表现一致？

**口播版（≥150 字）**：我在后端和前端分别做了收敛。后端的 `JwtAuthenticationFilter` 解析失败会清空 SecurityContext，把错误码放到 request attribute，然后交给认证入口返回统一响应，同时写一条包含 uri 和失败原因的运行日志。前端的 Axios 拦截器会在响应状态是 401 时清除本地 token，弹出“登录已失效，请重新登录”，再跳转登录页。我的目标不是让前端猜测所有错误，而是让后端稳定表达认证失败，前端只做统一处理。这样结果是用户体验比较一致，接口调试时也能从后端日志看到 token 解析失败的真实原因，而不是只看到页面跳回登录。

### Q3（频率：高）[AI/RAG/降级可观测]

**面试官问题**：你项目里的 AI 预分析具体怎么落地？失败时怎么办？

**口播版（第一人称，≥150 字，完整 STAR）**

我做 AI 模块时的场景是，校园报修可以用大模型辅助分类、判断优先级、生成故障摘要和派单建议，但主流程不能因为模型超时或返回格式异常而阻塞。我的任务是把 AI 做成辅助决策，而不是系统的单点依赖。实现上，学生提交工单后通过事务提交后的触发器启动预分析，`TicketAiAnalysisService` 会加载工单描述、报修图片、可用维修员列表和 RAG 召回片段，调用 LangChain4j 客户端生成 JSON。返回后我用 `ObjectMapper` 解析，并对优先级、风险等级、维修员 ID 做归一化校验。如果 JSON 解析失败或模型调用异常，我会标记 AI task 失败，写包含 `scenario`、`ticketId`、`aiTaskId`、`studentId` 的 `log.error`，再保存一条 fallback 分析，让管理员仍能人工审核派单。

**追问 1**：为什么 AI 输出不能直接展示给前端？

**口播版（≥150 字）**：我不会直接展示模型原文，因为模型输出有两个风险：一个是格式风险，比如它可能返回 Markdown、解释文字或字段缺失；另一个是业务风险，比如建议一个不存在的维修员 ID，或者把优先级写成系统枚举外的值。我的做法是把模型输出当成不可信输入处理，先要求它返回 JSON，再由服务端解析成固定 payload，并对枚举、数值范围、维修员列表做校验和归一化。比如 `suggestedWorkerId` 如果不在启用维修员列表里，我会记录 warn 日志并置空。这样最终前端拿到的是后端确认过的结构化建议，不会因为模型一次发散输出导致页面崩溃或错误派单。

**追问 2**：RAG 在这里起什么作用？如果向量库不可用呢？

**口播版（≥150 字）**：RAG 的作用是让 AI 建议基于系统已有的维修知识，而不是只靠大模型常识。我的知识库模块会把管理员维护的文档切片，配置 embedding provider 和 Milvus 向量库；搜索时先按分类过滤启用文档，优先走 Milvus 召回，再把 chunk ID hydrate 回数据库里的标题和正文，最后通过 rerank 取 Top N。如果 Milvus 没有命中或不可用，系统还能回退到数据库关键词评分，按照中文二元片段和分词命中计算 score，再 rerank。这样结果是演示环境即使没有稳定向量库，也能保持基本检索能力；同时日志会记录向量同步、搜索失败的场景和上下文，便于解释降级原因。

### Q4（频率：中高）[事务/文件/RustFS]

**面试官问题**：报修图片和维修结果图片是怎么处理的？怎么保证和工单关系一致？

**口播版（第一人称，≥150 字，完整 STAR）**

我处理图片模块时的约束是，图片文件不能直接存数据库，但也不能只把 URL 给前端自由提交，否则会出现文件归属不清、越权绑定、对象存储成功但业务失败的问题。我的目标是把对象存储和业务绑定拆开，同时通过事务保证元数据关系正确。具体实现上，`FileService.upload` 先校验文件大小和 MIME，再上传到 RustFS，成功后写 `FileMetadata`，初始 `bizType` 是 `TEMP`。学生创建报修或维修员提交结果时，再调用绑定接口，把临时文件校验为当前用户上传、未删除、未被其他业务占用，然后改成 `TICKET_REPORT_IMAGE` 或 `TICKET_RESULT_IMAGE` 并绑定 `ticketId`。如果上传失败，我用 `log.error(..., exception)` 记录 bucket、objectKey、上传人和原始文件名，结果是排查对象存储问题时有完整上下文。

**追问 1**：如果创建工单时图片绑定失败，会不会留下脏数据？

**口播版（≥150 字）**：我在创建工单时把核心数据库操作放在事务里，包括插入工单、替换报修图片、更新工单图片 URL 和写流转记录。如果传入了不存在的图片文件 ID，`requireBindableFiles` 会抛出业务异常，事务会回滚，避免出现工单已创建但图片关系不完整的情况。项目里我还专门写了集成测试，先统计工单数量，再提交一个带不存在图片 ID 的创建请求，最后断言工单数量没有变化。这里需要说明的是，RustFS 对象本身不参与数据库事务，所以更稳的演进方向是增加定时清理 `TEMP` 文件或上传会话机制，用来处理用户上传后未提交工单的临时对象。

**追问 2**：文件访问权限怎么控制？别人能不能绑定我的图片？

**口播版（≥150 字）**：我在绑定阶段做了两类校验。第一类是角色校验，比如报修图片只能学生绑定，维修结果图片只能维修员绑定；第二类是文件归属校验，`requireBindableFiles` 会根据 fileIds 查元数据，确认文件未删除、上传人是当前用户，并且当前还是 `TEMP`，或者已经绑定在同一个业务上。别人拿到 fileId 也不能绑定，因为 uploaderId 对不上会抛 `FILE_ACCESS_DENIED`。查看工单文件时，我还会调用 `ticketService.ensureTicketReadable`，让管理员、工单所属学生、分配维修员之外的人无法读取列表。结果是文件权限跟工单权限保持一致，不靠前端隐藏按钮来防越权。

### Q5（频率：中高）[测试/可观测/工程质量]

**面试官问题**：你怎么证明这个系统不是只做了页面，而是工程上可靠？

**口播版（第一人称，≥150 字，完整 STAR）**

我证明工程可靠性时，主要从测试、日志和边界场景三个维度入手。场景上，校园报修的风险不在于能不能新增一条记录，而在于跨角色、跨状态、多模块协作时是否稳定。我的任务是把这些高风险路径变成可验证结果。测试方面，我写了 `TicketFlowIntegrationTest`，用 MockMvc 和 JWT token 跑完整流程：学生提交、管理员派单、维修员接单、提交结果、学生评价，并断言最终状态、流转记录和评价数据。同时我覆盖了另一个学生越权查看、未分配维修员接单、重复派单返回非法状态、图片绑定失败事务回滚等场景。日志方面，项目要求 catch、fallback、第三方失败都要写运行日志，并包含业务场景和关键 ID。这样面试时我可以说明：我不仅实现了功能，还验证了失败路径。

**追问 1**：业务日志、审计日志、运行日志有什么区别？

**口播版（≥150 字）**：我会把它们分成三个用途。业务日志或业务记录，比如 AI task、AI analysis、工单流转表，主要是给系统页面和业务追溯看的，回答“这个工单经历了什么”。审计日志更偏管理动作留痕，比如管理员催办、派单、修改状态，回答“谁在什么时候做了什么操作”。运行日志是给排障和可观测看的，回答“代码为什么失败、第三方为什么不可用、兜底为什么发生”。所以我不会用数据库记录替代运行日志。比如 AI 解析失败时，数据库可以记录 task failed 和 fallback 结果，但运行日志必须用 `log.error(..., exception)` 保留异常栈，并带上 `scenario`、`ticketId`、`aiTaskId`、`studentId`，否则线上只能看到降级结果，看不到真实原因。

**追问 2**：你还会补哪些测试或指标？

**口播版（≥150 字）**：如果继续补，我会优先补三类。第一类是 AI/RAG 的失败矩阵，包括模型超时、非法 JSON、RAG 空召回、Milvus 不可用、rerank 失败，验证都能写运行日志并返回可用兜底结果。第二类是性能和容量指标，比如 1 万到 5 万工单下统计接口 P95 响应时间、列表分页耗时、RAG Top 3 命中率，这些目前我会标注为（待测），并说明怎么测。第三类是前端端到端测试，用学生、维修员、管理员三个账号走关键路径，验证路由守卫、按钮状态和 401 重新登录。这样结果是项目从“能演示”继续走向“能交付”，面试官追问质量保障时也有具体路线。

### Q6（频率：中）[前端/三角色工作台]

**面试官问题**：前端部分你做了哪些工程化设计？

**口播版（第一人称，≥150 字，完整 STAR）**

我前端面对的场景是，同一个系统要服务学生、维修员、管理员和数据大屏，页面入口、菜单、接口和状态展示都不一样。如果全部堆在一个页面里，后续维护会很困难。我的任务是让前端按角色组织，同时和后端权限模型保持一致。实现上，我用 Vue 3、Vue Router、Pinia、Element Plus 和 Axios 封装基础工程。`router/index.ts` 中每个路由带 `meta.role`，全局守卫会检查 token、拉取当前用户，并在角色不匹配时跳回该角色默认首页。`utils/request.ts` 统一注入 `Authorization`，处理业务 code、超时和 401。页面上拆分学生首页、我的工单、维修工作台、管理后台、知识库和数据大屏。结果是前端结构和业务角色一致，联调时也更容易定位是路由、接口还是后端权限问题。

**追问 1**：前端路由守卫和后端权限校验会不会重复？

**口播版（≥150 字）**：我认为它们不是重复，而是职责不同。前端路由守卫负责用户体验，比如学生登录后不应该看到管理员菜单，也不应该点进管理员页面后才发现接口报错；后端权限校验负责安全边界，因为前端代码可以被绕过，接口可以被 Postman 直接请求。所以我在前端根据 `meta.role` 和 Pinia 里的当前角色做跳转，在后端 Service 里继续按 `CurrentUser` 校验角色和数据归属。结果是正常用户路径更顺滑，恶意或误操作请求也不会越权。面试里我会强调，任何涉及数据权限的判断都以后端为准，前端只是减少无效操作。

**追问 2**：如果前端请求失败，你怎么做统一处理？

**口播版（≥150 字）**：我在 Axios 实例里做统一处理，而不是每个页面手写一遍。请求拦截器读取本地 token 并加到 `Authorization: Bearer` 头里；响应拦截器先判断后端统一响应里的 `code`，非 0 就用 Element Plus 提示业务错误并 reject；如果 HTTP 状态是 401，就清除 token、提示登录失效并跳转登录页；如果是超时，就提示稍后重试；其他网络异常则展示后端 message 或默认文案。这样页面组件只关心业务数据，不需要重复处理认证和错误提示。结果是联调时错误表现稳定，也降低了页面代码复杂度。

## 量化与验证补充（面试诚实口径）

- 工单主流程、越权、非法状态、图片绑定失败事务回滚：已有集成测试覆盖，可继续扩展更多状态矩阵。
- AI 成功率、fallback 次数、RAG Top 3 命中率、统计接口 P95：当前没有实测数据，面试中应标注为（待测），可用 JMeter/k6、Mock AI 响应、固定知识库问答集来测。
- 前端 E2E：当前可手工演示三角色路径，建议后续用 Playwright 覆盖登录、创建工单、派单、处理、评价、知识库管理和大屏访问。
