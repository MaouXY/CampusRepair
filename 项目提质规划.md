# 校园报修系统项目提质规划

## 1. 提质目标

当前系统已经具备学生报修、管理员审核派单、维修员处理、学生评价、图片上传、AI 预分析、RAG 知识库等核心能力。下一阶段的目标不是继续堆普通 CRUD，而是把系统提升为更像“校园后勤政务 OA”的协同平台。

提质目标分为四类：

1. 管理端更像 OA：突出待办、督办、复核、派单、统计和留痕。
2. 维修端更像工作台：突出今日任务、接单处理、退回、结果提交和绩效。
3. AI 更深度介入流程：从“手动点分析”升级为“自动建议、辅助派单、沉淀知识”。
4. 工程化更稳：状态机、权限、幂等、日志、组件复用、测试覆盖更完整。

## 2. 建议版本路线

### V1：OA 协同增强版

目标：让系统从“能跑流程”升级为“像真实后勤 OA 在用”。

重点功能：

- 管理端待办中心
- 工单督办与 SLA
- 维修员退回工单
- 维修端今日任务
- 管理/维修内部协同备注
- 操作日志与审计记录

推荐优先做这个版本，因为它最能提升项目观感，也最容易在答辩演示中体现业务完整性。

### V2：AI 辅助调度版

目标：让 AI 不只是一个分析按钮，而是嵌入工单流转。

重点功能：

- 学生提交后自动 AI 预分析
- AI 推荐分类、优先级、摘要
- AI 推荐维修员
- AI 结合 RAG 给维修建议
- AI 风险预警

### V3：数据治理与知识沉淀版

目标：让系统具备长期运行价值。

重点功能：

- 典型工单自动生成知识库草稿
- 月度维修报告
- 满意度与绩效分析
- 高发故障、高发地点统计
- RAG 检索升级为真实向量模型

## 3. 高性价比功能规划

## 3.1 管理端待办中心

### 业务价值

管理端现在主要是工单列表，像普通管理后台。政务/OA 系统更强调“我的待办”和“处理闭环”，因此管理端首页应该从静态说明升级为待办工作台。

### 页面设计

路径建议：

- `/admin/dashboard`

页面模块：

- 待审核工单
- 维修员退回工单
- 超时未接单
- 超时处理中
- 待学生确认
- 今日新增工单

展示方式：

- 顶部 KPI 卡片：待审核、待督办、处理中、已完成
- 中部待办列表：按紧急程度排序
- 右侧公告/系统提醒
- 下方趋势简图：近 7 日工单量、完成率、超时率

### 后端接口建议

新增接口：

```text
GET /api/v1/admin/dashboard/todos
GET /api/v1/admin/dashboard/summary
GET /api/v1/admin/dashboard/trends?days=7
```

返回字段示例：

```json
{
  "pendingReviewCount": 8,
  "returnedCount": 2,
  "overdueAssignedCount": 3,
  "overdueProcessingCount": 1,
  "waitingConfirmCount": 5,
  "todayCreatedCount": 12
}
```

### 验收标准

- 管理员登录后第一屏能看到待办数量。
- 点击待办卡片能跳转到对应状态筛选的工单列表。
- 超时工单有醒目标识。
- 数据刷新后能反映最新工单状态。

## 3.2 工单督办与 SLA

### 业务价值

真实 OA 系统不能只记录状态，还要管理时效。SLA 可以让系统体现“服务质量管理”。

### 状态规则建议

建议先做简单规则：

- 待审核超过 2 小时：待审核超时
- 已派单超过 4 小时未接单：接单超时
- 处理中超过 24 小时未提交结果：处理超时
- 待确认超过 48 小时：确认超时

后续可升级为配置表。

### 数据库建议

新增表：

```sql
repair_sla_rule
```

字段建议：

- `id`
- `status`
- `limit_minutes`
- `enabled`
- `created_at`
- `updated_at`

工单表可增加：

- `sla_deadline_at`
- `overdue`
- `overdue_minutes`

也可以不落表，先按状态时间实时计算。

### 前端展示

- 工单列表增加“时效”列。
- 超时显示红色标签：`已超时 3 小时`
- 即将超时显示黄色标签：`剩余 30 分钟`
- 管理端详情增加“督办”按钮。

### 后端接口建议

```text
POST /api/v1/admin/tickets/{ticketId}/urge
GET /api/v1/admin/tickets?page&size&status&overdue=true
```

### 验收标准

- 管理员能筛选超时工单。
- 超时工单在列表中有明显标识。
- 督办操作会写入流转记录和操作日志。

## 3.3 维修员退回工单

### 业务价值

管理员可能派错专业、派错区域，维修员应该能退回未接单工单。这个能力会让流程更真实。

### 状态流转

新增状态：

```text
RETURNED = 维修员退回
```

主流程：

```text
PENDING_REVIEW -> ASSIGNED -> PROCESSING -> WAITING_CONFIRM -> COMPLETED
```

退回分支：

```text
ASSIGNED -> RETURNED -> ASSIGNED
RETURNED -> REJECTED
```

规则：

- 只有 `ASSIGNED` 状态可退回。
- 只有被分配的维修员可退回。
- 退回必须填写原因。
- 管理员可对 `RETURNED` 工单重新派单或驳回。

### 后端接口

维修端：

```text
POST /api/v1/worker/tickets/{ticketId}/return
```

入参：

```json
{
  "reason": "该工单属于网络设备故障，当前维修员负责水电维修，建议转派。"
}
```

管理端：

```text
POST /api/v1/admin/tickets/{ticketId}/assign
POST /api/v1/admin/tickets/{ticketId}/reject
```

需要允许 `PENDING_REVIEW` 和 `RETURNED` 两种状态。

### 前端改动

维修员详情页：

- `ASSIGNED` 状态显示：
  - `接单处理`
  - `退回工单`
- 点击退回弹出原因输入框。

管理端：

- 工单状态筛选增加“维修员退回”。
- 抽屉详情展示退回原因。
- 重新派单时可选择其他维修员。

### 日志要求

退回失败、权限异常、状态非法必须写运行日志，至少包含：

- `scenario`
- `ticketId`
- `workerId`
- `currentStatus`
- 真实异常信息

### 验收标准

- 维修员能退回自己未接单的工单。
- 不能退回别人派给自己的工单之外的工单。
- 已接单处理中不能退回。
- 管理员能重新派单退回工单。
- 流转记录完整展示。

## 3.4 维修端今日任务工作台

### 业务价值

维修员现在主要看列表，像数据表。今日任务工作台能让维修端更像一线办事系统。

### 页面设计

路径建议：

- `/worker/dashboard`

页面模块：

- 今日待接单
- 今日处理中
- 即将超时
- 已完成
- 我的绩效摘要

任务列表排序规则：

1. 紧急优先级
2. 即将超时
3. 创建时间更早
4. 同地点聚合

### 后端接口

```text
GET /api/v1/worker/dashboard/tasks
GET /api/v1/worker/dashboard/summary
```

返回字段示例：

```json
{
  "assignedCount": 3,
  "processingCount": 2,
  "overdueCount": 1,
  "completedTodayCount": 5,
  "avgProcessMinutes": 86
}
```

### 前端体验

- 顶部展示四个任务数字。
- 中间是“今日任务”列表。
- 每条任务显示状态、优先级、地点、摘要、剩余时间。
- 主按钮根据状态变化：
  - `ASSIGNED`：接单
  - `PROCESSING`：提交结果
  - `WAITING_CONFIRM`：查看详情

### 验收标准

- 维修员无需进入完整列表，也能知道今天该处理什么。
- 高优先级和超时任务醒目展示。
- 点击任务能进入工单详情。

## 3.5 AI 自动预分析前置

### 业务价值

现在管理端需要手动点击“AI 预分析”。更好的方式是：学生提交工单后，系统自动触发 AI 预分析，管理员打开审核页面时直接看到建议。

### 流程设计

```text
学生提交工单
-> 保存工单
-> 上传并绑定图片
-> 触发 AI 预分析任务
-> 保存 AI 分析结果
-> 管理员审核时直接读取最新分析结果
```

### 实现方式

阶段 1：

- 学生提交工单成功后，前端或后端触发分析。
- 推荐后端异步触发，避免前端等待太久。

阶段 2：

- 引入任务表状态：
  - `PENDING`
  - `RUNNING`
  - `SUCCESS`
  - `FAILED`
  - `FALLBACK`

阶段 3：

- 管理端抽屉显示 AI 分析状态。
- 如果失败，允许管理员手动重新分析。

### 后端建议

新增：

```text
POST /api/v1/admin/tickets/{ticketId}/ai-analysis/retry
GET /api/v1/admin/tickets/{ticketId}/ai-analysis/latest
```

也可以在工单详情接口中直接带最新 AI 分析。

### AI 输入

文本：

- 地点
- 分类
- 描述
- 联系方式是否完整
- 历史相似工单
- RAG 知识片段

图片：

- 使用 `file_metadata.public_url`
- 通过 LangChain4j URL 图片输入传给模型

注意：

- `publicUrl` 必须能被云端 AI 访问。
- 如果是 `localhost` 或内网地址，AI 无法拉取图片。
- 生产环境建议使用公网 RustFS 地址或预签名 URL。

### 验收标准

- 学生提交后，管理端打开工单能看到 AI 建议。
- AI 建议包含分类、优先级、摘要、风险、处理建议。
- AI 失败时有运行日志和页面提示。

## 3.6 AI 派单建议

### 业务价值

管理员派单时选择维修员比较机械。AI 可以根据工单类别、地点、维修员负载、历史绩效推荐合适人选。

### 推荐依据

基础规则：

- 维修员启用状态
- 工单分类匹配
- 当前待接单/处理中数量
- 近期完成数量
- 平均处理时长
- 好评率
- 是否有超时记录

AI 输入：

- 工单描述
- AI 故障分析结果
- 可选维修员列表
- 各维修员当前负载
- 历史相似工单处理人

### 后端接口

```text
POST /api/v1/admin/tickets/{ticketId}/dispatch-suggestion
```

返回示例：

```json
{
  "recommendedWorkerId": "10002",
  "recommendedWorkerName": "李师傅",
  "reason": "该工单属于水电维修，李师傅当前处理中工单较少，且近期水电类工单平均处理时长较短。",
  "confidence": 0.82
}
```

### 前端展示

在派单表单“维修员”字段上方显示：

- 推荐维修员
- 推荐原因
- 一键填入按钮

### 验收标准

- 管理员能看到推荐人选和原因。
- 点击“一键填入”能自动选择维修员。
- AI 不可用时不影响人工派单。

## 3.7 AI 维修建议

### 业务价值

维修员接单后，系统可以结合 RAG 知识库给出排查步骤，提升专业感。

### 触发时机

- 维修员打开详情页
- 或接单成功后自动生成

### 输出内容

- 可能原因
- 排查步骤
- 所需工具/备件
- 安全注意事项
- 是否建议升级优先级

### 后端接口

```text
GET /api/v1/worker/tickets/{ticketId}/repair-advice
POST /api/v1/worker/tickets/{ticketId}/repair-advice/generate
```

### 前端展示

维修详情页增加“AI 维修建议”卡片：

- 接单前可查看简版建议
- 接单后显示完整处理建议
- 支持复制到处理备注

### 验收标准

- 维修员能看到可执行的排查步骤。
- 建议内容来自工单信息 + RAG 知识库。
- AI 失败时显示“暂无建议”，不阻塞处理流程。

## 3.8 典型工单自动沉淀进知识库

### 业务价值

维修知识库如果只靠管理员手工维护，会很空。系统可以从已完成的典型工单中生成知识库草稿。

### 触发条件

满足以下条件之一：

- 学生评价 4 分及以上
- 工单处理结果完整
- 有图片和详细备注
- 某类故障重复出现多次

### 流程设计

```text
工单完成
-> AI 判断是否适合沉淀
-> 生成知识库草稿
-> 管理员审核
-> 入库并切片
-> 同步到 Milvus
```

### 数据表建议

新增：

```sql
rag_knowledge_draft
```

字段：

- `id`
- `source_ticket_id`
- `title`
- `content`
- `category_id`
- `status`
- `created_by_ai`
- `reviewed_by`
- `reviewed_at`
- `created_at`
- `updated_at`

状态：

- `PENDING_REVIEW`
- `APPROVED`
- `REJECTED`

### 管理端页面

路径建议：

- `/admin/rag/drafts`

功能：

- 查看 AI 生成的知识草稿
- 编辑标题和内容
- 审核入库
- 驳回草稿

### 验收标准

- 完成工单后可生成知识草稿。
- 管理员审核后进入知识库。
- 入库后可被 AI 维修建议和预分析检索到。

## 4. 工程化提质规划

## 4.1 统一状态机

后端应集中管理状态流转，避免控制器或服务随意修改状态。

建议维护状态矩阵：

```text
PENDING_REVIEW -> ASSIGNED
PENDING_REVIEW -> REJECTED
ASSIGNED -> PROCESSING
ASSIGNED -> RETURNED
RETURNED -> ASSIGNED
RETURNED -> REJECTED
PROCESSING -> WAITING_CONFIRM
WAITING_CONFIRM -> COMPLETED
```

验收：

- 非法流转全部失败。
- 每次状态变化都写 `repair_ticket_flow`。
- 状态失败日志包含 `ticketId`、当前状态、目标状态、操作人。

## 4.2 幂等保护

需要重点处理：

- 重复提交评价
- 重复派单
- 重复接单
- 重复退回

建议：

- 后端状态机天然拦截重复操作。
- 前端按钮提交时禁用。

## 4.3 统一前端业务组件

建议抽取：

- `TicketStatusTag`
- `TicketPriorityTag`
- `TicketFlowTimeline`
- `TicketImageViewer`
- `TicketBasicInfo`
- `TicketActionBar`

收益：

- 三端展示一致。
- 枚举不会到处裸露。
- 后续加状态只改一处。

## 4.4 日志与审计

运行日志：

- 记录系统排障信息。
- 失败、异常、降级、熔断必须写日志。
- 使用 `log.error(..., exception)` 保留堆栈。

业务审计：

- 记录谁做了什么。
- 管理员派单、驳回、督办、转派、基础数据修改都要留痕。

建议新增表：

```sql
operation_audit_log
```

字段：

- `id`
- `operator_id`
- `operator_role`
- `biz_type`
- `biz_id`
- `action`
- `before_snapshot`
- `after_snapshot`
- `remark`
- `created_at`

## 4.5 测试规划

后端测试：

- 学生提交工单
- 管理员派单
- 维修员接单
- 维修员退回
- 管理员重新派单
- 维修员提交结果
- 学生评价
- 非法状态失败
- 权限越权失败

前端测试：

- 三类角色菜单隔离
- 状态筛选
- 图片上传和预览
- 管理端审核抽屉
- 维修端今日任务
- AI 预分析失败提示

## 5. 数据库执行规划

后续如果实施这些功能，建议新增 SQL 文件：

```text
09_schema_oa_enhancement.sql
10_seed_oa_enhancement.sql
```

建议执行顺序：

```text
01_schema_base.sql
02_schema_ticket.sql
03_schema_management.sql
05_schema_file.sql
06_schema_ai_rag.sql
04_seed_data.sql
07_fix_rag_chunk_cleanup.sql
08_seed_rag_rich_knowledge.sql
09_schema_oa_enhancement.sql
10_seed_oa_enhancement.sql
```

由用户手动执行 SQL。

## 6. 推荐实施顺序

### 第一优先级

1. 维修员退回工单
2. 管理端待办中心
3. 工单督办与 SLA
4. 维修端今日任务

理由：

- 不依赖复杂 AI。
- 最能提升 OA 感。
- 对演示效果提升明显。
- 和现有工单状态机强相关，适合先补。

### 第二优先级

1. AI 自动预分析前置
2. AI 派单建议
3. AI 维修建议

理由：

- 能体现 AI 赋能。
- 和已有 AI/RAG 模块复用度高。
- 可以逐步降级，不影响主流程。

### 第三优先级

1. 知识库草稿沉淀
2. 月度维修报告
3. 绩效与满意度分析
4. 真实向量模型接入

理由：

- 更像长期运营系统。
- 实现周期略长。
- 更适合作为答辩亮点或后续扩展。

## 7. 答辩展示建议

建议演示路径：

1. 学生提交带图片的报修。
2. 系统自动 AI 预分析。
3. 管理员在待办中心看到待审核工单。
4. 管理员查看图片、AI 建议和派单建议。
5. 管理员派单。
6. 维修员在今日任务中看到新任务。
7. 维修员认为派错专业，退回工单。
8. 管理员看到退回原因，重新派单。
9. 维修员接单，查看 AI 维修建议，提交处理结果和图片。
10. 学生确认并评价。
11. 系统将典型工单生成知识库草稿。
12. 管理员审核入库。

这条演示链路能同时体现：

- 多角色协同
- OA 待办
- 状态流转
- 图片能力
- AI 多模态
- RAG 知识库
- 业务闭环
- 数据沉淀

## 8. 总结

下一阶段不建议继续零散加页面，而应该围绕“后勤政务 OA + AI 辅助调度”建设。最值得先做的是管理端待办中心、SLA 督办、维修员退回工单、维修端今日任务和 AI 自动预分析。这些功能能显著提升系统成熟度，也能让项目从普通毕业设计 CRUD 系统变成更完整的校园后勤智能协同平台。
